# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` and are not checked in. Regenerating
# them is how the association files, the entitlement and the package name stay in step
# with app.config.ts rather than drifting from it.
/ios
/android

# Metro
.metro-health-check*

node_modules/
*.log
