{
  "name": "henad",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "dev:web": "pnpm --filter @henad/web dev",
    "build:web": "pnpm --filter @henad/web build"
  }
}
